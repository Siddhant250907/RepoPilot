# Broken Python Demo Project

A Python command-line utility with intentional syntax errors, missing requirements, and broken unit tests.

### Bug Description
Running `pytest` produces import errors and failing assertions.

### Verification Criteria
RepoPilot must locate the broken test, trace the root cause to an incorrect return type, and resolve it.
